#!/bin/bash

#add checking for vm provider, and installing correect plugins

# sets up your environment to spin up Malcolm VM using Vagrant, Ansible, and Virtualbox
# not tested and probably is missing something

# sudo apt-get -y update && sudo apt-get -y install vagrant virtualbox ansible sshpass

